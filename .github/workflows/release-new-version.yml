name: Create a New Release

on:
  workflow_dispatch:
    inputs:
      bump:
        description: 'Version component to bump (auto, patch, feature, yearly, or rc)'
        required: true
        type: choice
        default: auto
        options:
          - auto
          - patch
          - feature
          - yearly
          - rc

run-name: "Release ${{ inputs.bump }} bump on ${{ github.ref_name }}"

permissions:
  contents: write

jobs:
  release:
    uses: valkyrjaio/.github/.github/workflows/_create-php-release.yml@983eb086a57533c52b7ce86a29b2d537aad838ed
    secrets: inherit
    with:
      bump: ${{ inputs.bump }}
      dependencies: |
        [
          {"name": "Root Packages", "command": "root-outdated-check"},
          {"name": "Psalm",  "command": "psalm-outdated-check"}
        ]
      info-class-path: src/Psalm/Constant/PsalmInfo.php
      info-class-name: PsalmInfo

  update-refs:
    name: Update Workflow Refs
    needs: release
    uses: valkyrjaio/.github/.github/workflows/_update-workflow-refs.yml@983eb086a57533c52b7ce86a29b2d537aad838ed
    secrets: inherit
    with:
      source-repo: ${{ github.event.repository.name }}
